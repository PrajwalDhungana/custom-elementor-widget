window.addEventListener( 'elementor/init', () => {

        var elementorTravelTimeView = elementor.modules.controls.BaseData.extend({
            onReady() {
                
            }
        })

} );